# qa-powers

AI 驱动的 UI 自动化测试技能。Skill 在 `skills/<name>/SKILL.md`。兼容 Claude Code、Codex、Pi 及其它 Agent Skills 宿主。

安装与宿主差异见 [.agents/hosts.md](.agents/hosts.md)。invocation 与可选 skill 见 [.agents/invocation.md](.agents/invocation.md)。

用户提到 UI 测试、设计用例、跑用例、测试报告时，Call the Skill tool with "using-qa-powers"。

职责：只测不修。细则见 `skills/run/SKILL.md` 硬约束 7。
